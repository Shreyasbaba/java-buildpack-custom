#!/bin/sh -x -v -s

#cli=`find /tmp -name jboss-cli.sh`

find /tmp -name '*.cli' -exec echo /tmp/staged/app/.java-buildpack/jboss_eap/bin/jboss-cli.sh -c --file={} \; | sort | /bin/sh -x -v -s

exit 0
